import React from "react";

function Resume () {
    return (
         <section>

            <div className="center" id='resume'>
                <h1 className="header">My Resume</h1>
            </div>

            {/* need to add resume link */}
         </section>
    );
}

export default Resume;